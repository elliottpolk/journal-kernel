---
description: "Materializes the Project Structure from a confirmed spec into real files and directories on the feature branch. Reads the spec, confirms the scaffold plan with the user, executes, and commits. Run after /spec, before /plan."
name: "scaffold"
agent: agent
---

This prompt's workflow instructions are defined in the canonical source file below. Read it now and follow every phase and rule it defines.

[WORKFLOW](.agentic/workflows/scaffold/WORKFLOW.md)
